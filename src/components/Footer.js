import React from "react";
import styled from "styled-components";

function Footer() {
  const Container = styled.div``;

  return (
    <Container>
      <p> Acá van el footer</p>
    </Container>
  );
}

export default Footer;
